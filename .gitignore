# secrets never touch the repo
.env
*.env
*token*
*secret*
*.key
*.pem
# VIP phrase lists — never commit these
viplist*.txt
phrases*.txt
# live setup (the .example ships, yours stays local)
bouncer.conf
# runtime
__pycache__/
*.log
*.pid
.DS_Store
